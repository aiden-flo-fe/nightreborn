appId: com.aidenflo.nightreborn
productName: RecipeSetup   # 띄어쓰기 없는 제품명
directories:
  output: dist
files:
  - build/**/* 
  - dist/**/*

win:
  target:
    - nsis
  artifactName: recipe-setup-${version}.${ext}   # ✅ 산출물 이름 패턴 지정